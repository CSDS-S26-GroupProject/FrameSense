# Face-shape Flask API — local dev + Render (Linux)
#
# Production (Render): Gunicorn (Linux only — do not swap for Windows on the server)
# Local Windows:       flask run   OR   waitress-serve (see README)

flask>=3.0,<4.0
flask-cors>=4.0,<6.0
gunicorn>=22.0,<24.0
waitress>=3.0,<4.0
numpy>=1.24,<3.0
opencv-python-headless>=4.8,<5.0
mediapipe>=0.10.0
joblib>=1.3,<2.0
